# Kingsmarch — full-game acceptance matrix

**Date:** 2026-09-17 · **Author:** [Cursor] (cloud; model: Claude Fable 5) · **Coordinator:** Morgan Sterling · **Owner of record:** Adam
**Commissioned by:** the 2026-09-16 games-completion handoff (Adam authorized turning Kingsmarch "100%" into finite, owner-visible acceptance rows).
**Revision 2 (2026-09-17, Codex document review):** [Cursor] (cloud; model: Claude Fable 5.1). Docs-only corrections: A1/A2/B1/B6/B7/C1/C2 now carry the historical date + SHA of their live evidence and say **NOT RUN** for current-tip live acceptance; dependency IDs that resolved to nothing (`O1`, `J1`) now point at real rows (H6, F1); external IDs (`G-05`, `D-13`) are labeled as such; §5 counts are stated explicitly and match the tables; B10 is split into a local drill (B10a) and hosted operational restore (B10b). **No gate was rerun and no new live pass is claimed in this revision** — §1 numbers are still the revision-1 runs.
**Revision 3 (2026-09-17, B10a executed):** [Cursor] (cloud; model: Claude Fable 5.1). **One new current-tip live PASS: B10a**, run at `38610e0` (`cursor/b10a-local-restore-drill` = `3673a9a` + `scripts/b10a-local-restore-drill.mjs`; `server/` byte-identical to `main @ 9b478db`), evidence in `docs/verification/2026-09-17-b10a-local-restore-evidence.md`. Rows touched: B10a, §5 counts, §6, §7, M8. No other row changed status; B10b stays MISSING; §4 untouched.
**Revision 4 (2026-09-20, HTTP-only live re-drive executed):** [Cursor] (cloud; model: Claude Fable 5.1). The §7 task "re-drive the 2026-08-29 scratch-DB HTTP exercise" was **run at `21b917b`** (`cursor/http-live-redrive-40e9` = `19bf897` + `scripts/http-live-redrive.mjs`; `server/` and `packages/` byte-identical to `main @ 9b478db`), 14/14 steps PASS, exit 0, evidence in `docs/verification/2026-09-20-http-live-redrive-evidence.md`. Rows now carrying **current-tip live PASS: A1, B1, B7, C1, C2, and the server half of A2**. A2's Roblox-side reconcile, B6, E10, F6, G1 remain historical-only (Studio). §5 counts, §6, §7 updated to match. No other row changed status; §4 untouched.
**Revision 5 (2026-09-20, B8 kill -9 / WAL recovery drill executed):** [Cursor] (cloud; model: Claude Fable 5.1). The §7 task "write the kill-9 WAL recovery test" was **run at `69b7548`** (`cursor/b8-kill9-wal-drill-37b7` = [PR #13](https://github.com/Adamdesgns/kingsage-remaster/pull/13) tip `00b8c8e` + `scripts/b8-kill9-wal-recovery-drill.mjs`; `server/` and `packages/` byte-identical to `main @ 9b478db`), 11/11 steps PASS, exit 0, evidence in `docs/verification/2026-09-20-b8-kill9-wal-recovery-evidence.md`. **B8 moves from NOT RUN to PASS (current-tip live, SIGKILL only — power loss is not simulated and the row says so).** C2 gains one live sub-claim (a `PREREQUISITE_MISSING` refusal). §5 counts, §6, §7, M3 updated to match. No other row changed status; **no Studio row moved**; §4 untouched.
**Revision 6 (2026-09-20, H1/H2 door drill executed):** [Cursor] (cloud; model: Claude Fable 5.1). Not a §7-assigned task; chosen as the cheapest remaining HTTP-only claim that was tests-only. The production rate-limit defaults (5/min per address, 30/min per player — literals in `server/src/http.ts`, never before driven: the unit test injects 3 and 2 against a fake clock) and the contract-shape guard were **run at `03a9f41`** (`cursor/h1-h2-door-drill-0901` = [PR #14](https://github.com/Adamdesgns/kingsage-remaster/pull/14) tip `4a5b9fb` + `scripts/h1-h2-door-drill.mjs`; `server/`, `packages/` and `roblox/` byte-identical to `main @ 9b478db`), 10/10 steps PASS, exit 0, evidence in `docs/verification/2026-09-20-h1-h2-door-drill-evidence.md`. **H1 and H2 move from tests-only to current-tip live PASS**; M1's local sub-claim "rapid registrations throttled" is shown on loopback (M1 itself stays NO — hosting). H3's row gains a live observation (the key-gated link route is unthrottled, as coded). §5 counts, §6, §7, M1 updated to match. No other row changed status; **no Studio row moved**; §4 untouched.
**Revision 7 (2026-09-20, C2 building ladder drill executed):** [Cursor] (cloud; model: Claude Fable 5.1). The second item on revision 6's "HTTP-only claims still resting on tests alone" list — C2's other prerequisite rules and completion of buildings other than the Timber Camp — was **run at `11f14d6`** (`cursor/c2-building-ladder-drill-6ff7` = [PR #15](https://github.com/Adamdesgns/kingsage-remaster/pull/15) tip `35095c7` + `scripts/c2-building-ladder-drill.mjs`; `server/`, `packages/` and `roblox/` byte-identical to `main @ 9b478db`), 12/12 steps PASS, exit 0, evidence in `docs/verification/2026-09-20-c2-building-ladder-evidence.md`. Seven disposable worlds; the six gated buildings reached their thresholds via `KINGSAGE_DEV_SEED_LEVEL` (production DEV-ONLY knob, its Freehold guarantee checked live). **C2 now carries live evidence for every prerequisite rule refused and five of six accepted at threshold, the 10-order queue limit, level stacking, `waiting`-unpaid orders, all 13 max-level refusals, and three more completed building types (Stone Quarry, Warehouse, Farm) with two chained starts at the exact completion instant.** Not shown: the Academy's acceptance (the knob caps it at 3 before HQ 10 is reachable), completion of the other nine types, construction offline catch-up. C3 gains a live observation. §5 parenthetical, §6, §7 updated. **No row changed status** (C2 was already counted as a live PASS); **no Studio row moved**; §4 untouched.
**Revision 8 (2026-09-20, D1–D3/E1 marches + fog drill executed):** [Cursor] (cloud; model: Claude Fable 5.1). The third item on revision 7's "HTTP-only claims still resting on tests alone" list — D1–D3 fog/scouting and E1 march launch — was **run at `6e5c9bc`** (`cursor/d1-d3-e1-marches-fog-drill-d1ee` = [PR #16](https://github.com/Adamdesgns/kingsage-remaster/pull/16) tip `6ade713` + `scripts/d1-d3-e1-marches-fog-drill.mjs`; `server/`, `packages/` and `roblox/` byte-identical to `main @ 9b478db`), 10/10 steps PASS, exit 0, ~60 s of real march timers, evidence in `docs/verification/2026-09-20-d1-d3-e1-marches-fog-evidence.md`. One disposable world, three readers on both doors (two Roblox-linked players, one web-registered "watcher" for the event stream), troops from `KINGSAGE_DEV_SEED_ARMY` (production DEV-ONLY knob, its Freehold guarantee checked live). **D1, D2, D3 and E1 move from tests-only to current-tip live PASS**; E2's server half (recall) gains a live observation; C1's row now states the cap arithmetic (9.43 h at fixture levels, longer at every seed level) that makes the cap branch unreachable without a labelled simulation — **C1 did not change status**. **No battle was fought** (the world was stopped 119.8 s before the waiting attack's auto-resolve; 0 battle sessions). Three observations recorded, not judged (offline-arrival return leg measured from catch-up; a scouted village is not told; a besieged village is not told). §5 counts, §6, §7, F4 updated. **No Studio row moved**; §4 untouched.
**Revision 9 (2026-09-20, battle-settle drill executed):** [Cursor] (cloud; model: Claude Fable 5.1). The first item on revision 8's "HTTP-only claims still resting on tests alone" list — **the battle itself** — was **run at `2456b3a`** (`cursor/battle-settle-http-drill-58fc` = [PR #17](https://github.com/Adamdesgns/kingsage-remaster/pull/17) tip `d6334c7` + `scripts/battle-settle-drill.mjs`; the script file that ran is byte-identical to `aec462f`'s — `2456b3a` plus one log-string line that was uncommitted when the run started, stated in the note; `server/`, `packages/` and `roblox/` byte-identical to `main @ 9b478db`), 10/10 steps PASS, exit 0, 214 s of real timers including two full 120 s waits at the walls, evidence in `docs/verification/2026-09-20-battle-settle-evidence.md`. One disposable world; the attacker on the Roblox door, the defender and a watcher on the web door; troops from `KINGSAGE_DEV_SEED_ARMY`. Three attacks: an **attended** one (`battle.open` accepted with the report's version, five field orders and the cap, `battle.resolve`) that beat a Freehold, took 2 prisoners and brought 90 wood home into the barn; and two **unattended** ones the server fought at `auto_resolve_at` — a defeat at a player's walls and a victory over the garrison the first battle had emptied. **Every stored outcome was recomputed by the drill with game-core's `resolveBattle` from the frozen row and matched byte for byte.** **E4 and E6 move from tests-only to current-tip live PASS; E5's orders half, D3's acceptance path, C1's loot route, B6's fight (not its conquest) and M2's server-side "offline auto-resolve" clause gain live evidence.** Not driven: `battle.retreat`, rams' permanent wall drop, conquest, and **the B8 mid-settle kill — left as the follow-up with an exact recipe in the evidence note**. Five observations recorded, not judged (auto-resolve stamps are catch-up time; a wiped column still walks home as "0 troops returned"; two plan records per attack; Freehold notifications nobody reads; a defender still hears nothing before the fight). §5 counts, §6, §7 updated. **No Studio row moved**; §4 untouched.
**Scope:** the **intended game** — not only the practice slice. One file Adam can open to see exactly what is done, what is missing, and who owes what.

**Code baselines this matrix is pinned to (both re-gated in this session — see §1):**

| Line | Tip | What it is |
|---|---|---|
| `main` | `9b478db` | The intended game. Includes the 2026-08-29 Phase A merge (audit P0/P1 fixes, full recruit picker, research UI, Recall, the Herald, AI tick in dev loop, hosting-as-config). |
| Practice line | `cursor/practice-fort-held-slice-08e7` @ `0b7a34a` ([PR #10](https://github.com/Adamdesgns/kingsage-remaster/pull/10), draft) | Current practice-siege candidate. Stack: `main` → `feat/practice-siege-codex` @ `add2cd2` → [PR #7](https://github.com/Adamdesgns/kingsage-remaster/pull/7) @ `41d541e` → [PR #8](https://github.com/Adamdesgns/kingsage-remaster/pull/8) @ `bdcef2e` → [PR #9](https://github.com/Adamdesgns/kingsage-remaster/pull/9) docs / [PR #10](https://github.com/Adamdesgns/kingsage-remaster/pull/10) code. None merged; merge is Claude's call on Adam's word. |

**Reconciliation note (no duplicated work):** PRs #7/#8/#9/#10 were inspected before writing this. #7 = Phase 1 teaching lesson; #8 = touch-scroll fix (`TouchScroll.luau` — **not on `main`**); #9 = docs/runbook reconciliation on `bdcef2e`; #10 = FORT HELD explanation slice on `bdcef2e`, the newest code tip. This matrix duplicates none of them; it maps them.

## How to read the status column

- **IMPLEMENTED** — the code exists at the named SHA.
- **TESTED** — automated tests current at that SHA fail if the behavior is deleted (this repo's honesty bar).
- **VERIFIED LIVE** — dated live evidence (Studio session, live HTTP exercise) at a named SHA.
- **PARTIAL** — some of the row works; the notes say which part.
- **BROKEN** — exists but confirmed wrong.
- **MISSING** — not built at all.
- **NOT RUN** — the acceptance evidence for the **current** tip does not exist. Older evidence is labeled historical and does not transfer.
- **BLOCKED** — cannot advance without the named gate (a device, a human, an account, or Adam's word).
- **DECISION-HELD** — waiting on Adam's explicit yes/no. Nobody may invent the answer.

A row may carry both: **VERIFIED LIVE (historical: date, SHA)** for the code claim *and* **live NOT RUN** for the current tip. The historical label never certifies `9b478db`; it only says the behavior was once exercised live, and where.

Honest-evidence rules inherited from the briefing and the 2026-09-16 handoff: historical gate numbers (core 104 / server 139 / scroll 11 as previously recorded, the 2026-08-23 Studio audit, the 2026-08-29 audit's scratch-DB HTTP exercise at `c9e1e5c`, the 2026-09-11 assisted S-01) are **history, not current results**. Every claim below either carries current evidence or says NOT RUN.

**Dependency IDs:** every cross-reference in the Owner/Notes columns is a row id in this file (A1…H9, S-01…S-10, G-01/G-02, M1…M8, or a §4 decision id). Audit section numbers are written `§7.x`/`§16.x`. Two ids from the practice line's opening-decisions doc are quoted where the source text uses them and are marked *(external)*; they are not rows here.

**Exact prerequisite for any Studio row below** (replaces the earlier blanket "Adam's PC gates"): a Windows machine with Roblox Studio and Rojo installed and a checkout of the named tip — in practice Adam's PC ("Bot Door"), operated either by Adam at the keyboard or by an agent in a granted computer-control session (the 2026-09-11 evening PC follow-up was one such session). Steps: `powershell -ExecutionPolicy Bypass -File roblox/start-dev.ps1 -BuildOnly -Play` → `roblox/WorldGame-dev.rbxlx`; a disposable world server on `127.0.0.1:4178` with key `dev-secret-local-0001` (`KINGSAGE_DATABASE_PATH` pointing at a fresh file; **never** the 4178 world already running); `SecretConfig.luau` from the example; Studio **Allow HTTP Requests** ON; `/api/health` returns `{ok:true, service:"kingsage-world", contractVersion:1}` before Play. Cloud agents cannot do this; a PC session can. Rows that need a **physical phone** (S-09, G4) additionally need Adam's device and are marked so.

---

## 1. Gate health — rerun in THIS session (2026-09-17, cloud Linux, Node 22.14, Lune 0.10.5)

| Gate | `main` @ `9b478db` | Practice tip @ `0b7a34a` |
|---|---|---|
| `npm run check:types` | **clean** | **clean** |
| `npm run test:core` | **92/92, 0 fail** | **105/105, 0 fail** |
| `npm run test:server` | **114/114, 0 fail, 0 skipped** (Lune contract test executed) | **139/139, 0 fail, 0 skipped** |
| `npm run test:luau` | 72 rules + 7 spike sims + syntax, **0 failed** | 42 syntax · 72 rules · 7 sims · 25 connections · 6 client audits · 272 practice contracts · 28 bridge · 14 planner scenarios · 54 wiring · 11 touch-scroll — **0 failed** |
| `npm run check:practice-persistence` | n/a (script exists only on practice line) | **exit 0** — disposable DB, `defenderWin`, identical replay, durable rows unchanged |
| Rojo place build / Studio Play / phone | **NOT RUN** — not possible from cloud; needs the PC session described in "Exact prerequisite for any Studio row" above | **NOT RUN** — same |

These are fresh runs, not quotes of the PR #10 handback. All Luau "scenario" checks are runtime stubs by their own admission — they are **not** Studio or visual proof.

---

## 2. The intended game — feature acceptance rows

Evidence pointers are `file:line` at `main @ 9b478db` unless another SHA is named. "Audit" = `docs/audits/kingsage-functionality-audit.md` (2026-08-29, taken at `c9e1e5c`); rows below are **reconciled against the four commits `main` has gained since** — several audit findings are now fixed and say so.

### 2A. Account, session, identity

| # | Acceptance row | Status | Owner | Evidence | Notes |
|---|---|---|---|---|---|
| A1 | Roblox UserId → account link, auto-create, idempotent rejoin | IMPLEMENTED + TESTED (current, §1); **VERIFIED LIVE (current tip, 2026-09-20, `21b917b`)** — re-drive steps 1–2: new UserId → `created:true` + seat `kingdom-1`; same UserId again → `created:false`, identical `playerId`/`kingdomId`; second UserId → different player + seat; wrong/missing key → 401 `BAD_KEY` with no link row written | — (done; Cursor ran it) / Claude reviews the record | `docs/verification/2026-09-20-http-live-redrive-evidence.md` steps 1–2, 10; `scripts/http-live-redrive.mjs`; `server/src/store.ts` link path; roblox-link tests | Server under test byte-identical to `main @ 9b478db`. Rejoin also re-verified **after a server restart** (step 10). |
| A2 | Reconnect reconciles fully from server authority (no client cache) | IMPLEMENTED + TESTED (current, §1); **server half VERIFIED LIVE (current tip, 2026-09-20, `21b917b`)** — after a graceful stop/restart the same UserId rejoins to the same player/kingdom and the full `/api/roblox/state` snapshot (jobs, army, notifications, version) is rebuilt from the DB alone; Roblox-side reconcile (client re-rendering from that pull): VERIFIED LIVE **historical** (2026-08-23 Studio audit), current-tip **NOT RUN** | — (server half done) / Morgan (Roblox-side reconcile in Studio, prerequisite above) | Evidence note steps 10–11; `roblox/src/server/WorldSession.luau` | Architecture A honored in code and now re-shown live on the server side at this tip. The client half still needs the Studio prerequisite. |
| A3 | Kick/seat race: a non-allowlisted player cannot claim a seat before the kick lands | BROKEN (known, P2) | Cursor | Audit §8.7; `roblox/src/server/init.server.luau` vs `WorldSession.luau` | Unfixed by Phase A. Needs a two-client drill to prove the fix. |
| A4 | Ban / moderation / player-reset tooling | MISSING | Cursor (build) / Adam (policy) | Audit §10.11 | Nothing exists. |

### 2B. World model, persistence, world lifecycle

| # | Acceptance row | Status | Owner | Evidence | Notes |
|---|---|---|---|---|---|
| B1 | Seeded deterministic world (50×50 fixture, 10 settlements) | IMPLEMENTED + TESTED (current, §1); **VERIFIED LIVE (current tip, 2026-09-20, `21b917b`)** — re-drive step 3: snapshot pull shows `world-7c7d5ae2` seed `gate-b-emberfall`, 50×50, 10 villages / 10 kingdoms (2 open seats claimed as human, 4 AI, 4 freehold); a **second fresh DB** on another port seeded the identical world id, seed and settlement id/coordinate layout | — (done; Cursor ran it) / Claude reviews | Evidence note step 3; `packages/game-core/src/fixture.ts` | Single world only. Open-seat villages are renamed after the claiming player, so names are player data, not fixture data. |
| B2 | Open seats: new players get a fresh start; AI never develops open seats | IMPLEMENTED + TESTED | — (done) | `1ed8d73`, migration `0012_open_seats.sql`, `server/test/freeholds.test.ts` + `ai-kingdoms.test.ts` | Resolves the audit's AI/open-seat overlap **in code**. |
| B3 | World capacity beyond 6 seats / settlement founding / `WORLD_FULL` story | MISSING + DECISION-HELD | Adam (shape) → Cursor | Audit §7.B, §16.2 | The world can only shrink. "50+ settlements per player" (CANONICAL-BRIEF) is unserved. |
| B4 | World lifecycle: win condition, world end, reset, seasons/shards | MISSING + DECISION-HELD | Adam (endgame shape, §16.8) → Cursor | Audit §7.B; `WorldStatus` declared in `contracts.ts`, never written | Archived prototype's 40% dominance + world cycling is the candidate design, unadopted. |
| B5 | Terrain affecting marches/defence | MISSING | Adam (whether at all) | Audit §7.B; existed only in archived `index.html` | Euclidean distance + slowest-unit pace is the whole story today. |
| B6 | Freeholds on-ramp (conquerable first rung) | IMPLEMENTED + TESTED (current, §1); **fought live at the current tip (2026-09-20, `2456b3a`)** — battle drill steps 1, 2, 4, 6, 8: Saltmarsh Freehold stands unarmed by the seed knob (the fixture's 10 Squires, wall 0, 400/350/250), is scouted (report kingdom "Saltmarsh Freehold", version 0), beaten by 10 Berserkers under the best plan (8 Squires dead, **2 surrendered and marched off with the attacker**, 90 wood looted, +24 war points), then looted again by an unattended 5-Squire column (77 wood + 48 stone from what was left); its kingdom collects both defeat notifications though nobody controls it; **still `freehold-3` afterwards — no Count rode, `applyConquest` never entered**; **conquest** VERIFIED LIVE **historical only** (2026-08-23 Studio audit — two-wave conquest at dev pacing; exact SHA not recorded, pre-`373b3ff`; record: `docs/HANDOFF-2026-08-24-fresh-chat.md`); current-tip live conquest **NOT RUN** | — (fight done; Cursor ran it) / conquest: Cursor (HTTP slice with `KINGSAGE_DEV_SEED_NOBLES`) / Morgan (Studio look) | `docs/verification/2026-09-20-battle-settle-evidence.md` steps 1, 2, 4, 6, 8; `fixture.ts`; freeholds tests | The first rung can be fought and looted over the wire at this tip; whether it can be **taken** at this tip is the untouched half — an HTTP conquest drill (Counts via the dev knob, two waves, Realm of Power 294 → capture) is the natural next slice and needs no Studio. |
| B7 | Restart persistence: jobs, versions, idempotency survive a **graceful** restart | IMPLEMENTED + TESTED (current, §1 — `gate-b`/`gate-c` restart tests); **VERIFIED LIVE (current tip, 2026-09-20, `21b917b`)** — re-drive step 10: SIGTERM exit 0, 50 s down, restart on the same DB: `/api/health` 200; same player/kingdom/village ids; the construction job (id, `completesAt`) identical; inbox row count unchanged; **replay of the pre-restart `commandId` returns the byte-identical stored result** and creates no second job | — (done; Cursor ran it) / Claude reviews | Evidence note step 10; audit §2 (historical) | Graceful only — B8 is the hard-kill case (kill -9 now PASS at `69b7548`, revision 5; power loss still NOT RUN). |
| B8 | Killed-process (kill -9 / power loss) WAL recovery | **PASS for kill -9 (current-tip live, 2026-09-20, `69b7548`)** — 11/11 steps, exit 0: two players burst 18 commands in ~80 ms, server `SIGKILL`ed on the 8th accept with `world.sqlite` a 4096-byte header and **1.47 MB of uncheckpointed `-wal`**; control copy of the main file without its WAL has **0 tables**; server restarted as first opener → `integrity_check` ok, `foreign_key_check` empty; **all 17 acknowledged commands (8 accepted + 9 stored rejections) replay byte-identically**; the 1 in-flight command is wholly absent (no inbox row, no job); job tables equal the ledger (6 construction, 2 recruitment, no orphans); resources reconcile to <1e-6; both 45 s recruits still landed at exactly `completesAt`. Repeatable: `node scripts/b8-kill9-wal-recovery-drill.mjs`. **Power loss NOT RUN** — SIGKILL discards the process, not the kernel page cache; fsync durability needs a VM/plug drill, not a signal | — (kill -9 done; Cursor ran it) / Claude reviews the record / power-loss: Claude (VPS session, after H6) | `docs/verification/2026-09-20-b8-kill9-wal-recovery-evidence.md` (verbatim log, ledger, file sizes, both state pulls); `scripts/b8-kill9-wal-recovery-drill.mjs`; audit §13.2 | Server under test byte-identical to `main @ 9b478db`. Not covered: kill during battle settlement / march arrival (audit M3's "kill -9 mid-battle-settle" — same `BEGIN IMMEDIATE … COMMIT` path, but no battle was in progress); the drill records where the kill landed rather than steering it (this run: before the in-flight commit). |
| B9 | Retention/pruning: events, inbox, notifications, battle rows | MISSING | Cursor | Only `DELETE` in store.ts is logout (`store.ts:891`) | Unbounded growth; deliberately out of Phase A scope. |
| B10a | **Local** backup + restore drill on a disposable world (proves the runbook's `VACUUM INTO` backup and the restore steps actually round-trip a WAL-mode SQLite world) | **PASS (current-tip live, 2026-09-17, `38610e0`)** — 6/6 steps, exit 0; negative control (naive `cp` instead of `VACUUM INTO`) FAILS at step 3 as it must. Repeatable: `node scripts/b10a-local-restore-drill.mjs` | — (done; Cursor ran it) / Claude reviews the record | `docs/verification/2026-09-17-b10a-local-restore-evidence.md` (verbatim log, both state pulls, exit codes); `scripts/b10a-local-restore-drill.mjs`; procedure: `docs/ops/vps-runbook.md` "Backups" + "Restore drill" | Server under test is byte-identical to `main @ 9b478db` (`git diff 9b478db 38610e0 -- server/` empty). Restored world came back at version **2** with the first (timber) job; the live world had reached version **3** with a second (quarry) job, which was gone after restore. Naive copy left a backup with **no tables** (schema still in the 943 KB `-wal`) — the runbook warning is real. Needs only Node 22 + `sqlite3` CLI. Drill as specified: (1) start the world server on a fresh `KINGSAGE_DATABASE_PATH` (path resolves relative to `server/`), throwaway `KINGSAGE_ROBLOX_KEY`, non-4178 port; (2) `/api/roblox/session` + one `village.build.queue` so the DB has a job and an inbox row; (3) `sqlite3 <db> "VACUUM INTO '<backup>'"` **while the server is running** (WAL mode — that is the point); (4) queue a second command so live state diverges from the backup; (5) stop the server, copy the backup over the DB, delete `-wal`/`-shm`, restart; (6) `/api/health` 200 and a `/api/roblox/state` pull showing the **first** job and world version, not the second. Record date, SHA, exact commands, and both state pulls in a dated `docs/verification/` note. Passing this does **not** close B10b. |
| B10b | **Hosted** operational restore: cron backup landing in `/home/kingsage/backups`, restore drill on the real box, `/api/health` + state pull after | MISSING (runbook written, never executed on a box) | Claude (VPS session) / Adam (box) | `docs/ops/vps-runbook.md` | Blocked behind hosting (H6). B10a passing (2026-09-17) means the procedure itself is proven locally; the hosted drill is now a repeat on real infrastructure, not a first try. Still not run. |

### 2C. Economy, buildings, recruitment, research

| # | Acceptance row | Status | Owner | Evidence | Notes |
|---|---|---|---|---|---|
| C1 | Resource production, warehouse caps, fractional carry, offline accrual | IMPLEMENTED + TESTED (current, §1); **VERIFIED LIVE (current tip, 2026-09-20, `21b917b`) for production, fractional carry and offline accrual** — re-drive steps 4, 12, 13: `villageEconomy` equals the game-core formulas at the village's levels (28/h, storage 1464, population 232); `whole + carry == baseline + 28/h × elapsed − spent` holds to <1e-6 for all three resources, both across a 50 s server-down window and over the 11+ minute construction wait, with whole units landing at the formula rate; **warehouse cap enforcement NOT hit live** (would need hours of accrual — unit-tested only); **resources arriving from OUTSIDE a village shown live (2026-09-20, `2456b3a`)** — battle drill steps 7, 10: a returning column's `loot_json` lands in the barn at exactly its `arrives_at` (1200 → **1290** wood, stone and iron untouched, the prisoners joining the garrison in the same instant), a second column adds 77 wood + 48 stone, and the final barn equals fixture + both loots + the 28/h trickle | — (done; Cursor ran it) / Claude reviews | Evidence note steps 4, 12, 13; `economy.ts`; gate-c tests; arithmetic in `docs/verification/2026-09-20-d1-d3-e1-marches-fog-evidence.md` header; loot: `docs/verification/2026-09-20-battle-settle-evidence.md` steps 7, 10 | The cap branch of `accrueVillage` is the one part of this row still resting on tests alone. Loot is now a proven route to bring resources in, but 90–125 per raid against a 264-unit gap makes it no faster than accrual for reaching the cap honestly. **Why it stays that way without a labelled simulation (worked 2026-09-20):** at fixture levels wood sits at 1200 against a 1464 cap and earns 28/h → **9.43 h** of real accrual to touch the cap (stone 16.6 h, iron 23.7 h); `KINGSAGE_DEV_SEED_LEVEL` lengthens it at every rung because storage grows 1.22×/level and production 1.17×/level (L2 17.8 h, L10 65.8 h, L99 385 h); no knob seeds resources; loot needs a settled battle. A stopped-server rewind of `last_materialized_at` would reach it in seconds and would be a simulation, to be labelled as such if ever run. |
| C2 | 13 building types: costs, prereqs, queue stacking, server timers, offline catch-up | IMPLEMENTED + TESTED (current, §1); **VERIFIED LIVE (current tip, 2026-09-20, `21b917b`) for cost, server timer, exact-time completion, idempotent replay, stale-version conflict and ownership** — re-drive steps 5–8, 14: `village.build.queue` timber deducted exactly {75, 90, 60}; timer 720 s = `buildingDurationSeconds`; replay byte-identical with no duplicate row; stale `expectedWorldVersion` → 409 `WORLD_VERSION_CONFLICT`; other player → 409 `FORBIDDEN` (both for a foreign village and a foreign `commandId`); **level 1 → 2 landed with its notification stamped exactly at `completesAt`** and production moved 28 → 33/h. Offline catch-up shown live for a *recruit* job (step 11: militia completed at its `completesAt` while the server was down); queue stacking shown live in B10a (quarry `waiting`) and again in B8 (5 `waiting` orders behind 1 `queued`, unpaid until they start); **one prerequisite refusal exercised live in B8 (`69b7548`): a Rampart order at Headquarters 1 → 409 `PREREQUISITE_MISSING` "Requires Headquarters level 2.", stored and replayed byte-identically after a kill**. **VERIFIED LIVE (current tip, 2026-09-20, `11f14d6`) for the building ladder** — all six prerequisite rules refused at standing level 1 with the exact "Requires … level N." (stored, replayed byte-identically, nothing built or charged); five of the six gates shown **opening at their threshold** in worlds seeded via `KINGSAGE_DEV_SEED_LEVEL` (Rampart at L2 paid 180/270/90 on a 1382 s timer; Smithy and Market at L3; Stable at L5; Workshop at L8), the rest still naming the missing prerequisite; prerequisites judged on **standing** levels (a `waiting` HQ order does not unlock the Rampart); unknown type → `INVALID_COMMAND`; **all 13 buildings at their caps → `INVALID_COMMAND` "already at its maximum level"**; **queue limit**: 9 stacked Farm orders with `targetLevel` 3..11, the 11th → 409 `QUEUE_FULL`, only the first paid; unaffordable orders accepted and parked `waiting` unpaid; **three more building types completed at exactly `completesAt` on a real clock — Stone Quarry (stone 28→33/h), Warehouse (storage 1464→1786), Farm (population cap 232→269)** — with notifications stamped at `completesAt`, the next order in each queue **started at that exact instant and paid then** (HQ 1→2 for 1080 s; Farm 2→3 for 1008 s at the level-2 cost 95/84/63), and every village's `whole + carry` reconciling to the formula to <1e-6. **Not shown live: the Academy's acceptance** (the uniform knob caps it at 3 before HQ 10 is reachable — tests-only), **completion of the other nine types** (Iron Mine, HQ, Barracks, Rampart, Smithy, Stable, Workshop, Academy, Market — HQ and Farm→3 were started live, not finished), and construction **offline catch-up** (the redrive showed it for a recruit only) | — (done; Cursor ran it) / Claude reviews | Evidence note steps 5–8, 11, 14; B8 evidence note step 2; `docs/verification/2026-09-20-c2-building-ladder-evidence.md` (12 steps, verbatim log); `scripts/c2-building-ladder-drill.mjs`; build-queue tests; audit §6.1 (historical) | Four building types completed live at this tip (Timber Camp at `21b917b`; Quarry, Warehouse, Farm at `11f14d6`). Observed, not judged (`11f14d6`): `waiting` orders are absent from the snapshot's `constructionJobs` (it selects `queued` only) and their `command.accepted` payload carries a placeholder `completesAt == startedAt` — see C3. |
| C3 | Build-queue cancel + client shows building costs/prereqs/queue detail | MISSING (cancel) / PARTIAL (client detail) | Cursor | Audit §7.C; ladder drill steps 4, 5 (`11f14d6`) | Phase A fixed the *recruit* side only. **Live observation 2026-09-20 (`11f14d6`):** the `/api/roblox/state` snapshot lists `queued` construction jobs only, so an accepted-but-`waiting` order (free, stored, acknowledged `command.accepted`) is invisible to the client until it starts, and its accepted payload carries a placeholder `completesAt == startedAt`. Recorded, not judged; whatever client detail is built must not trust that placeholder. |
| C4 | Full 11-troop recruitment from Roblox with server-truth costs/prereqs (troopCatalog) | IMPLEMENTED + TESTED, **not seen rendered** | Morgan/Adam (Studio look) | `1b104df`; `server/test/roblox-api.test.ts`; 9 new Luau rules | Supersedes the audit's "3 presets" finding. No human has ever seen this UI. |
| C5 | Smithy research reachable from Roblox (was server-only) | IMPLEMENTED + TESTED, **not seen rendered** | Morgan/Adam (Studio look) | `1b104df`; rules-check entries | Supersedes audit "DISCONNECTED". |
| C6 | Population cap is an invariant (recruit-over-cap via marched-out army) | BROKEN (known, P2) | Cursor | Audit §8.8; `store.ts` counts garrison+queued only | Unfixed by Phase A. |
| C7 | Player-to-player trade / market behavior / resource transfer | MISSING | Cursor (after F1) | Audit §7.D; Market building is a costed façade (§9) | The brief's second progression track has **no mechanics at all**. Blocked by alliances for donation (per brief). |
| C8 | Horses: production, cap, cavalry conversion | IMPLEMENTED + TESTED | — (done) | `horses.ts`; both horses test files | |
| C9 | Horses tradeable + raidable (the design's stated point of scarcity) | MISSING | Cursor (needs C7; raidable is a design call) | `horses.ts:18,33` admits it; settleBattle never touches herds | Silently inherited on conquest today. |

### 2D. Map, fog, intel

| # | Acceptance row | Status | Owner | Evidence | Notes |
|---|---|---|---|---|---|
| D1 | Fog of war in snapshots covers ALL fields incl. realm power + herds | IMPLEMENTED + TESTED (fixed since audit); **VERIFIED LIVE (current tip, 2026-09-20, `6e5c9bc`)** — marches drill steps 2, 6: for three readers on both doors (two Roblox-linked, one web) every one of the 9 foreign villages reads **zero** in resources, all 13 building levels, all 11 troop counts, `realmOfPower`, `realmOfPowerMax`, `horses`, `horsesMax`, keeping only name/coordinates/owner, while the DB rows hold 1200/1000/800, hq 1, a 34-troop seed army and realm power 294; own village equals its row; after a Spy lands, the report carries the target's exact army/buildings/realm power (294/294 = `settlementPoints`) and resources within the 28/h trickle, and **the same snapshot still fogs that village to zeros** | — (done; Cursor ran it) / Claude reviews the record | `docs/verification/2026-09-20-d1-d3-e1-marches-fog-evidence.md` steps 2, 6; `scripts/d1-d3-e1-marches-fog-drill.mjs`; `5b18b36`, migration 0011, `roblox-scouting.test.ts` | Audit §8.3 closed. Server under test byte-identical to `main @ 9b478db`. Observed, not judged (`6e5c9bc`): the scouted village gets no notification and no event that it was scouted. |
| D2 | Event stream fogged per reader (no free scouting off the wire) | IMPLEMENTED + TESTED (fixed since audit); **VERIFIED LIVE (current tip, 2026-09-20, `6e5c9bc`)** — marches drill steps 7, 10: a web-registered third kingdom's `/api/world/events?since=0` returns the 3 public `kingdom.claimed` rows and **none** of another player's `march.changed` / `scout.report.ready` rows while `local_world_events` holds them; a **live** `/api/world/stream` for that reader receives its `ready` frame and then **nothing** as the other player launches two attacks, and receives exactly the reader's own `march.changed` (full army) the moment it scouts; its later replay carries its own `scout.report.ready` and still nothing foreign | — (done; Cursor ran it) / Claude reviews the record | Evidence note steps 7, 10; `5cbb67c`; `gate-b.test.ts` | Audit §8.2 closed. The event routes exist only on the web door, so this is the web door's evidence; the Roblox snapshot's per-kingdom `marches`/`scoutReports` were shown alongside. Observed, not judged: `march.arrived` passes the filter for the attacker only — the besieged village learns nothing. |
| D3 | Scout-before-attack + intel freshness enforced | IMPLEMENTED + TESTED; **VERIFIED LIVE (current tip, 2026-09-20, `6e5c9bc`)** — marches drill steps 3, 7, 9: attack on an unscouted village → 409 `SCOUT_REQUIRED`, stored, replayed byte-identically, nothing left home (with `INVALID_TARGET`, `INVALID_ARMY` ×2, `INSUFFICIENT_TROOPS` alongside — 5 stored, 0 marches, version unchanged); the same attacker's attacks **accepted** once the report exists; at the walls, `battle.open` with a version never held → 409 `STALE_SCOUT_REPORT`, then after the defender sends one Spy out `battle.open` with **the report's own version** → 409 `STALE_SCOUT_REPORT` "The defender changed after your report…"; **0 battle sessions**; **acceptance path VERIFIED LIVE (2026-09-20, `2456b3a`)** — battle drill steps 4, 6: `battle.open` with the report's own version and an unchanged garrison → 200, session `open` frozen from the target's row; and the freshness rule shown **triggered by a battle**: after that battle emptied the garrison, `battle.open` of a second waiting army with the same report's version → 409 `STALE_SCOUT_REPORT`, while the server's own auto-resolve of that army does **not** re-check freshness (as documented) and fights the garrison as it stands | — (done; Cursor ran it) / Claude reviews the record | Evidence note steps 3, 7, 9; `docs/verification/2026-09-20-battle-settle-evidence.md` steps 4, 6, 8; `store.ts` gates (`launchMarchCore`, `intelIsCurrent`); battle tests | Both halves of the gate now live: refused when stale (three ways), accepted when current. |
| D4 | Interactive map (pan/zoom/tap targeting, marches drawn) | PARTIAL | Cursor | Audit §7.F — map tab is display-only dots; targeting works via distance-sorted lists | Phone usability of the lists NOT RUN. |

### 2E. Marches, combat, conquest

| # | Acceptance row | Status | Owner | Evidence | Notes |
|---|---|---|---|---|---|
| E1 | March launch: server-mustered army, slowest-unit pace, duplicate/outage-proof | IMPLEMENTED + TESTED; **VERIFIED LIVE (current tip, 2026-09-20, `6e5c9bc`)** — marches drill steps 4, 5, 7, 10: **mustered** — the Spy leaves the village row at acceptance (3 → 2), two attacks take 15 Berserkers + the Ram (20 → 5, 1 → 0), and home + in flight = the seed army at every checkpoint for both marching kingdoms; **paced** — over 22.8473 tiles `arrivesAt − departedAt` is exactly `marchDurationSeconds` from game-core: Spy 17 s, 10 Berserkers 39 s, 5 Berserkers + 1 Ram **58 s** (the Ram sets the pace), Spy's return 24 s; **duplicate-proof** — the launch `commandId` replayed with a stale version → byte-identical, 1 march, no second Spy, and byte-identical again after a restart; **outage-proof** — SIGTERM 1 s after launch with the process gone 18.5 s across the Spy's `arrivesAt`, DB row frozen `outbound` with no report the whole time, first pull after restart carries the report and notification stamped **exactly** at the original `arrivesAt`; homecomings `complete` with "N troops returned…" at `arrives_at`; attack rows carry `UNPLANNED_ATTACK_PLAN` with `auto_resolve_at` NULL until arrival, then arrivesAt + 120 s | — (done; Cursor ran it) / Claude reviews the record | Evidence note steps 4, 5, 7, 10; `store.ts` `launchMarchCore` / `materializeDueMarches`; `warfare.ts` `marchDurationSeconds`; `roblox-battles.test.ts` | Server under test byte-identical to `main @ 9b478db`. **Observed, not judged (`6e5c9bc`):** after an arrival caught up offline, the homeward leg is measured from the catch-up instant (2.725 s late here), not from the arrival the report is stamped with — the opposite of construction chaining (C2). Not covered: an *attack* arriving while the server is down; any battle. |
| E2 | March cancel/recall (server + two-tap Roblox Recall) | IMPLEMENTED + TESTED, **not seen rendered**; server half **observed live (2026-09-20, `6e5c9bc`)** — marches drill steps 8, 9: recall 3.065 s out → `returning` with a 3.065 s walk back from the recall instant, "6 troops turned for home." stamped there, column home at exactly its `arrives_at`; another kingdom's recall → 409 `FORBIDDEN`; recall at the walls → 409 `MARCH_COMMITTED` | Morgan/Adam (Studio look) | `189fa23` + `march-cancel.test.ts`; UI in `1b104df`; evidence note steps 8–10 | Supersedes audit "no cancellation". The row's open item is the Roblox Recall rendering — unchanged. |
| E3 | Support/reinforcement marches (cooperative defence) | MISSING | Cursor (after F1) | Audit §7.G — schema exists, path refuses | No cooperative play of any kind exists. |
| E4 | Deterministic class-vs-class combat: counters, wall, rams, surrender, conservation | IMPLEMENTED + TESTED; **VERIFIED LIVE (current tip, 2026-09-20, `2456b3a`)** — battle drill steps 6, 8: **three stored outcomes equal game-core `resolveBattle(frozen row)` byte for byte** (both armies, both level sets, wall, stock, plan, order count, seed — the seed itself checked as `sha256(worldId:marchId:openedAt)[:24]`); inside each outcome dead + standing + yielded = the army that fought, both sides; **surrender** live: an attack ≥ 3× the faced defence routs 10 Squires, **2 lay down arms** (the 25 % prisoner share of the casualties) and march home **with the attacker**, so the world's headcount is conserved across the change of side; a 5-Squire attack on 34 troops behind wall 1 is a total wipe that still costs the defender `(a/d)^1.5` of its garrison (1 Squire, 1 Berserker); an attack on an **empty** garrison wins against the base defence alone with no deaths; `planScore` 4 / 1 and `orderBonus` 0.10 / 0 ride the outcome as coded | — (done; Cursor ran it) / Claude reviews the record | `docs/verification/2026-09-20-battle-settle-evidence.md` steps 6, 8; `combat.ts`; `warfare.ts` `resolveBattle`; `battle-determinism.test.ts` | Certifies that the server fights with the shared rule and nothing else — not that the rule is right (§4's solo design calls). **Rams' permanent wall drop not driven live** (no rams sent). |
| E5 | Attended battle: orders capped, cosmetic positions, retreat exposure server-derived | IMPLEMENTED + TESTED (atMs fixed since audit); **orders half VERIFIED LIVE (current tip, 2026-09-20, `2456b3a`)** — battle drill step 5: sequences 1–5 accepted, an out-of-turn sequence → 409 `INVALID_ORDER` "The next valid battle order is sequence 3.", a foreign kingdom's order → 409 `BATTLE_CLOSED`, the 6th → 409 `ORDER_CAP_REACHED` "A battle accepts at most 5 field orders."; **a field order sent with a stale `expectedWorldVersion` is accepted** (the documented live-order exemption from the optimistic version check); orders move neither the world version nor the garrison; `acceptedOrders` 5 → `orderBonus` 0.10 in the settled outcome; `battle.open` grace shown: `auto_resolve_at` → 180 s from acceptance, never earlier than the arrival deadline | — (orders half done) / retreat: Cursor (HTTP) / positions: Morgan (Studio) | Evidence note steps 4, 5, 6; `7580daa`; `roblox-attend.test.ts` | Audit §8.4 closed — a client cannot buy survivors. **`battle.retreat` and its server-derived exposure NOT driven live**; the cosmetic positions are Studio. |
| E6 | Simultaneous sieges: second army fights survivors, loot conserved | IMPLEMENTED + TESTED (fixed since audit); **VERIFIED LIVE (current tip, 2026-09-20, `2456b3a`)** — battle drill steps 4, 6, 8: two armies at the same walls; with the first battle `open`, `battle.open` of the second → 409 `SIEGE_IN_PROGRESS` "Another army holds the field — wait for their battle to end."; after the first settles, the second's `battle.open` on the pre-battle report → 409 `STALE_SCOUT_REPORT`; the second then auto-resolves at its own unchanged deadline against **the garrison as the first left it** (here: empty) and its frozen stock equals the village row **after** the first loot came out — it looted 77 wood + 48 stone of what remained, never the same log twice | — (done; Cursor ran it) / Claude reviews the record | Evidence note steps 4, 6, 8; `0c8604c`; `simultaneous-attacks.test.ts` (3 scenarios) | Audit §8.1 (the P0) closed; also resolves decision §16.5 as "sequential". Live caveat: the first battle left **no** survivors, so "fights survivors" against a thinned-but-standing garrison is still tests-only; the refusal, the freshness trip and the loot conservation are live. |
| E7 | Night bonus wired into live battles | BROKEN (tested stub, wired to `false`) + DECISION-HELD | Adam (§16.6 wire-or-delete) → Cursor | `warfare.ts` call sites | False completion signal until decided. |
| E8 | Trebuchets damage a chosen building | BROKEN (stub — damage fn never called) + DECISION-HELD | Adam (§16.6) → Cursor | `combat.ts` `trebuchetDamage` | Currently a 500-attack infantry unit that slows the march. |
| E9 | Spy-vs-spy counter-intel phase | MISSING | Adam (whether at all) | Audit §7.H | |
| E10 | Realm-of-Power multi-wave conquest, transfer, aftermath | IMPLEMENTED + TESTED + VERIFIED LIVE (dev pacing, 2026-08-23, historical) | — (done) | `roblox-conquest.test.ts:430+` walks a live-HTTP campaign | Best-tested subsystem in the repo. |
| E11 | Conquest at **production** pacing has been played by a human | NOT RUN + DECISION-HELD | Adam (§16.3 keep/tune) | Audit §7.I — every live walk used dev seed knobs | 3–5 Counts × 900s × ~9k resources, never humanly paced. |
| E12 | Post-capture protection window | DECISION-HELD | Adam (§16.4) | Audit §7.I | Intended fragility or a window — do not invent. |

### 2F. Multiplayer, social, living world

| # | Acceptance row | Status | Owner | Evidence | Notes |
|---|---|---|---|---|---|
| F1 | Alliances: create/join/roles/war/peace/shared intel | MISSING | Cursor (schema→behavior→UI, audit stage 5) | Contract types rejected `INVALID_COMMAND` at the store allowlist | Blocks trade/donation (C7) per the brief. |
| F2 | Diplomacy / war-peace states | MISSING | Cursor (after F1) | Audit §10.2 | |
| F3 | Player-to-player chat | IMPLEMENTED (platform) | — (done by lock #4) | Roblox moderated chat is the locked answer | Custom world chat retired; alliance channel waits on F1. |
| F4 | Two-client ownership/privacy drill (visitor sees shells + zeroed everything) | NOT RUN | Morgan (execution) / Adam (second device if needed) | Named in audit M2 and the 2026-09-16 handoff; server side: marches drill step 2 (`6e5c9bc`) | Fog is tested server-side (D1/D2) and, since 2026-09-20, shown live over HTTP for three readers on both doors (`6e5c9bc`); the two-client *Studio* drill — what the visitor's screen actually shows — has never been run. |
| F5 | AI kingdoms: module (BUILD→RECRUIT→SCOUT→RAID, deterministic, player command paths) | IMPLEMENTED + TESTED | — (done) | `server/src/ai.ts`; `ai-kingdoms.test.ts` | |
| F6 | AI enabled where people actually play | IMPLEMENTED (dev loop) + VERIFIED LIVE (scratch drill 2026-08-29, historical) | — (done for dev) | `b7bad45` — `start-dev.ps1` sets `KINGSAGE_AI_TICK_MS=45000`; HANDBACK.md drill | Production enablement rides the VPS runbook (H6). |
| F7 | AI conquest / AI-vs-AI / Freehold behavior / offline catch-up | MISSING / PARTIAL | Cursor (audit stage 6) | Audit §7.K | AI resumes but does not catch up; never conquers. |
| F8 | Notifications delivered in-game (the Herald) | IMPLEMENTED + TESTED, **not seen rendered** | Morgan/Adam (Studio look) | `1b104df`; Luau rule "the herald renders the realm's notifications" | Supersedes audit "notifications never reach Roblox". Read/unread state still MISSING. |
| F9 | Battle/scout reports render with archive/retention | PARTIAL | Cursor | Audit §7.M — capped 5/4, no archive, no read state | |

### 2G. Roblox client & mobile baseline

| # | Acceptance row | Status | Owner | Evidence | Notes |
|---|---|---|---|---|---|
| G1 | Core-loop screens (village, war table, battle scene, reports, holding, error states) | IMPLEMENTED + TESTED (Luau gates) + VERIFIED LIVE only at 2026-08-23 audit (historical) | Morgan/Adam (current Studio pass owed) | `roblox/src/client/`; audit §7.N | Everything merged since 2026-08-29 (recruit picker, research, Recall, Herald) has **never been seen rendered** — named "the next Studio session's first job" in HANDBACK.md. |
| G2 | War-table touch/wheel scroll works on mobile | IMPLEMENTED + TESTED on practice line only; **NOT on `main`**; Studio NOT RUN | Claude (merge decision) / Morgan (S-07) | `TouchScroll.luau` @ `bdcef2e` (PR #8); 11 touch-scroll stub checks | The 2026-09-11 session that could not wheel-scroll is the bug, not a pass. Merging PR #8's line is the path to fixing this on `main`. |
| G3 | Touch targets ≥ 44px on busiest buttons | PARTIAL (fixed in practice UI; audit found sub-44px elsewhere) | Cursor | Audit §7.N; practice 320/390 checks | |
| G4 | Phone performance measured (200-troop budget, real device) | NOT RUN + BLOCKED (physical phone = Adam) | Adam (measurement) / Cursor (instrument done) | `feat/slice0-phone-measure` instrument; audit §7.Q | The repo's #1 self-declared unknown. `MAX_SOLDIERS=200` is an assumption. |
| G5 | Army/garrison management view, settings surface | MISSING | Cursor | Audit §7.N | |

### 2H. Security & operations (the "anyone can reach it" wall)

| # | Acceptance row | Status | Owner | Evidence | Notes |
|---|---|---|---|---|---|
| H1 | Rate limits: auth 5/min/address, commands 30/min/player | IMPLEMENTED + TESTED (fixed since audit); **VERIFIED LIVE (current tip, 2026-09-20, `03a9f41`) at the production defaults on a real clock** — auth door: attempts #1–5 from one address answered on merit (four 201 registrations and a wrong-password 401), **#6 (a registration) 429 `RATE_LIMITED` with two seats still unclaimed**, #7 (a correct login) 429; same login from `127.0.0.2` → 200 in the same second; +12.5 s restores exactly one attempt, +60.5 s restores all five. Command door: 30 commands in 214 ms answered on merit, #31/#32 → 429 `command.rejected` `RATE_LIMITED`, **nothing stored for a 429**; heartbeat pulls 200 throughout; p2 independent; +2.6 s restores one command and the once-refused `commandId` is judged on merit and stored (not poisoned) | — (done; Cursor ran it) / Claude reviews the record | `docs/verification/2026-09-20-h1-h2-door-drill-evidence.md` steps 2–4, 6–9; `scripts/h1-h2-door-drill.mjs`; `3508b92`; `rate-limit.test.ts` | Defaults still **untuned by real play** — this certifies that the coded numbers are the running numbers, not that they are good. Observed, not judged: no `Retry-After` on the 429. Not covered: behaviour behind a reverse proxy (the door would key on the proxy's address unless the client address is forwarded — an H6 runbook item); the Roblox client's rendering of a 429 (Studio). The key-gated link route `/api/roblox/session` is **not** throttled (as coded; see H3). |
| H2 | commandId validated (1–128 char string; no silent-replay trap) | IMPLEMENTED + TESTED (fixed since audit); **VERIFIED LIVE (current tip, 2026-09-20, `03a9f41`)** — missing / empty / 129-char / numeric `commandId` and missing / array / non-string-type `command` → 400 `INVALID_CONTRACT`; non-JSON and JSON-array bodies → 400 `INVALID_JSON`; 1 000 193-byte body → 413 `BODY_TOO_LARGE`; unlinked user → 404 `UNKNOWN_ROBLOX_USER`; **inbox 0 rows after all of it**; a 128-char id passes the door and is judged on merit; malformed requests consume no rate-limit allowance (the shape check runs first) | — (done; Cursor ran it) / Claude reviews the record | Evidence note steps 5, 7; Phase A; audit §8.5 closed | |
| H3 | Per-player auth to the world server (today: one shared key = act-as-anyone) | MISSING (accepted on loopback; **P0 the day it is hosted**) | Cursor (design) / Claude (review) | Audit §12.1; door drill step 6 (`03a9f41`) | Must land with or before H6 exposure. **Live observation 2026-09-20:** 8 rapid `/api/roblox/session` calls took no 429 — the link route rides the shared key and is deliberately outside the per-address door, so a leaked key can claim seats as fast as the wire carries requests. That is this row's problem, not H1's. |
| H4 | Missing-secret failure is loud, not an infinite hang | IMPLEMENTED (fixed since audit) | — (done) | Phase A: warns loudly once a minute; audit §8.6 closed | Server side of the same posture shown live at `03a9f41` (door drill step 10): with `KINGSAGE_ROBLOX_KEY` unset every `/api/roblox` route answers 503 `ROBLOX_DISABLED` and links nothing; health and the web door keep working. The Roblox-side warning itself is Luau (Studio). |
| H5 | Health endpoint touches the DB; session cookie `Secure`; session sweep | MISSING (all three) | Cursor | `http.ts:154-157` is still a static literal; cookie re-observed without `Secure` at `03a9f41` (door drill step 1) | Small, audit-listed hardening. |
| H6 | Hosting: reachable world server (VPS, TLS, systemd, backups) | BLOCKED (Adam: DigitalOcean API token + Porkbun domain) → then Claude runs the runbook | Adam → Claude | `docs/HANDOFF-2026-08-29-phase-b-vps.md`; `docs/ops/vps-runbook.md`; bind/base-URL already config (`13127d9`) | **The load-bearing blocker**: until this, nobody but Adam's household can play at all. |
| H7 | CI running the full gates on every PR | MISSING | Cursor | Audit §7.P | All gates are green (§1); nothing runs them automatically. |
| H8 | Gates decoupled from the frozen `mobile-rebuild` zombie | MISSING | Cursor | Audit §11.2 — `check-types.mjs` borrows its tsc; gate scripts grep it; server serves its dist | Standing gate-breakage risk. |
| H9 | Shippable name (can never be "KingsAge") | DECISION-HELD | Adam (§16.9) | CANONICAL-BRIEF | Blocks anything public, including the VPS place going beyond private. |

---

## 3. Practice slice — S-01…S-10 mapped, honestly

Pack of record: `docs/verification/2026-09-10-practice-studio-test-pack.md` (practice line; latest copy at `0b7a34a`, which also updated S-01/S-03 expected copy — FORT HELD now carries a `Why:` headline, loss denominators `8 of 18 · 7 of 14 · 5 of 10`, and `Defenders: 10 of 43 fell.`). Studio rows must be judged on a place rebuilt from **`0b7a34a`** (or, minimum, `bdcef2e` for S-07). **Historical results do not certify this tip**: the 2026-09-11 S-01 PASS was on older `41d541e` and needed Adam-assisted navigation past the scroll bug.

| Row | What it proves | Current-tip status | Owner | Evidence |
|---|---|---|---|---|
| S-01 Reset teaching plan (FORT TAKEN 5/5/4) | The one-tap win teaches | **NOT RUN** on `0b7a34a`/`bdcef2e` | Morgan (Studio) | Automated pin green (§1). Historical assisted PASS on `41d541e` only — not transferable. |
| S-02 Three drawn routes | Freehand drawing works | **NOT RUN** | Morgan (Studio) | — |
| S-03 No-gate-team failure (FORT HELD 8/7/5 + why) | The lesson's causality | **NOT RUN** live | Morgan (Studio) | Automated pins green incl. new headline/denominators (PR #10; §1). Live checklist: 2026-09-12 reconciliation note (practice line). |
| S-04 One-target causality (Gate→Keep only change) | Same routes, different outcome | **NOT RUN** live | Morgan (Studio) | Automated causality pin green. |
| S-05 Invalid input | Rejection names squad + problem | **NOT RUN** live | Morgan (Studio) | Automated planner checks green (probe rejection text verified §1). |
| S-06 Reset / retry / interrupt / reasons | No resurrected results | **NOT RUN** live | Morgan (Studio) | Automated planner checks green. |
| S-07 Drawing vs scrolling (TouchScroll) | The 2026-09-11 scroll bug is fixed | **NOT RUN** — the named **next required acceptance**, first row to run | Morgan (Studio, boot runbook on practice line) | 11 touch-scroll stub checks green (§1). |
| S-08 320/390 layouts | ≥44px targets, wrapping, V/A/R cues | **NOT RUN** live | Morgan (Studio) | Automated 320/390 checks green. |
| S-09 Physical phone | Real-device truth | **NOT RUN** + BLOCKED (physical phone; **Adam-written only** — emulation is explicitly not this row) | Adam | — |
| S-10 Stateless boundary probe | Practice grants nothing durable | **PASS (current)** — rerun this session on `0b7a34a`, exit 0 | — (done, repeatable) | §1 persistence probe. |
| G-01 Five unfamiliar players (D-13 *(external — practice-line opening-decisions doc)*) | Comprehension unaided | **OPEN** + BLOCKED (humans) | Adam (recruits testers) / Morgan (protocol) | One assisted S-01 does not count. |
| G-02 Opening-design contract | D-01/D-02-storage/D-08/D-09/D-10 + first-war rule | **DECISION-HELD** (Adam-written only; silence is not approval) | Adam | `docs/plans/2026-09-10-opening-decisions-for-adam.md` (practice line). |

---

## 4. Unresolved design choices (nobody may invent these)

| ID | Question | Status | Notes |
|---|---|---|---|
| **D-02 / OPEN-21** | How do two shielded newcomers ever begin PvP? Recommendation on record: **mutual first-war challenge** (both confirm; one paired accepted attack removes both shields; no timers, no toggles). | **DECISION-HELD — Adam's yes/no. This matrix records the question and deliberately does not answer it.** | Until answered, real PvP and G-05 *(external — the practice line's opening-decisions doc uses this id; it is not a row here)* stay closed. Timed expiry / voluntary drop / unshielded starters are not allowed paths. |
| §16.2 | World capacity & placement after 6 seats (bigger fixture / founding / multiple worlds) | DECISION-HELD | Shapes B3. |
| §16.3 | Conquest pacing at production timers | DECISION-HELD | E11. |
| §16.4 | Post-capture protection window | DECISION-HELD | E12. |
| §16.6 | Trebuchets & night bonus: wire or delete | DECISION-HELD | E7/E8 are false signals until decided. |
| §16.8 | World endgame (adopt archived 40% dominance + cycling?) | DECISION-HELD | B4. |
| §16.9 | The name | DECISION-HELD | Blocks anything public. |
| §16.1 hosting / §16.5 sieges / §16.7 AI-vs-open-seats | — | **RESOLVED** since the audit | Hosting: DO VPS chosen, account + payment ready (token/domain still owed → H6). Sieges: sequential, in code (`0c8604c`). Open seats: in code (`1ed8d73`). |
| Five solo design calls | Muster-all armies; 3 squads vs spec's ~10–20; surrender at 3×; 2-minute deadline; unplanned-attack fallback | OPEN (design review never done) | They work and are tested; none validated as *design*. |
| D-01 / D-08 / D-09 / D-10 | Opening contract recommendations | DECISION-HELD (bundled with G-02) | Reversible; recommendations recorded 2026-09-10. |

---

## 5. What "100%" actually means — milestone scoreboard

The audit's binary milestones (§15), scored today. **All eight are unmet: 0 met, 3 PARTIAL (M3, M4, M6), 5 NO (M1, M2, M5, M7, M8).** This is the finite list Adam asked for.

**Artifact counts (revision 2, so the summary matches the tables):** §2 holds **63 feature rows** (A 4 · B 11 incl. B10a/B10b · C 9 · D 4 · E 12 · F 9 · G 5 · H 9); §3 holds **12 practice rows** (S-01…S-10, G-01, G-02); §4 holds **10 decision lines**, of which 1 is RESOLVED and 1 is OPEN design review, the rest DECISION-HELD. Rows carrying a **current-tip live PASS: 17** (S-10 on `0b7a34a`; B10a on `38610e0`, revision 3; **A1, B1, B7, C1, C2 and the server half of A2 on `21b917b`, revision 4** — C1 minus the warehouse-cap branch; C2 for one completed building type there, extended at **`11f14d6`, revision 7** to every prerequisite rule, the queue limit, all max-level refusals and three more completed types, minus the Academy's acceptance and the nine types never completed live, as its row says; **B8 on `69b7548`, revision 5** — kill -9 only, power loss not simulated; **H1 and H2 on `03a9f41`, revision 6** — H1 at the coded defaults on loopback, not behind a proxy and not tuned; **D1, D2, D3 and E1 on `6e5c9bc`, revision 8** — fog on both doors, event fog on the web door, the scout gate and freshness refusals, and a mustered/paced/replay-proof/outage-proof march, with **no battle fought** and `battle.open`'s acceptance untouched, as the rows say; **E4 and E6 on `2456b3a`, revision 9** — the fight recomputed from the frozen row byte for byte, surrender, conservation, the sequential-siege refusal and loot conservation across two lootings, with E6's "survivors" caveat on the row; and, on the same run without changing a row's count, **E5's orders half, D3's acceptance path, C1's loot route, B6's fight and M2's server-side auto-resolve clause**). Rows carrying a **VERIFIED LIVE that is historical only: 5** (A2's Roblox-side half, B6's **conquest**, E10, F6, G1) — none of them certifies `9b478db`; B6's conquest and E10 could be reached over HTTP with `KINGSAGE_DEV_SEED_NOBLES`, the other three need the Studio prerequisite. Everything else Studio- or device-dependent is NOT RUN.

| Milestone | One-line test | Met? | What stands in the way (rows above) |
|---|---|---|---|
| M1 Hosted | Fresh phone on cellular joins the published private place; wrong key gets 401; rapid registrations throttled | **NO** — the two server-side clauses are now shown live **on loopback** (wrong key → 401 `BAD_KEY` at `21b917b`; sixth registration in a minute → 429 at `03a9f41`); the phone-on-cellular clause is hosting | H6 (Adam token/domain), H3, H5 |
| M2 Loop for real people | A kid on their own device: scout → offline auto-resolve → in-game report → recruit an axe → cancel a march; two-client fog drill passes | **NO** — every server-side clause is now shown live over HTTP: "scout", "report" and "cancel a march" at `6e5c9bc`; **"offline auto-resolve" at `2456b3a`** (two attacks nobody attended fought by the server 57 ms and 236 ms after their `auto_resolve_at`, the battle report in both kingdoms' snapshots and the defender's live stream); "recruit an axe" at `21b917b`/`69b7548`; everything a device shows is not | H6, F4, G1 Studio pass, G4 |
| M3 Persistent world | Concurrent sieges conserved (✅ done in code); kill-9 consistent; 7th player gets a designed answer | **PARTIAL** — E6 done and now **live** (`2456b3a`: `SIEGE_IN_PROGRESS`, second army loots only what the first left); B8 kill-9 drill PASS at `69b7548` (build/recruit writes; **the audit's "mid-battle-settle" variant still not run** — the battle drill leaves an exact recipe for it; power loss not run) | B8 (battle-settle variant, power loss), B3 |
| M4 Depth | Research from Roblox raises a level (✅ built, unseen); trebuchet hits a building; night attack means something | **PARTIAL** | C5 Studio look, E7, E8 |
| M5 Social | Alliance, reinforcement, 500-wood transfer | **NO** | F1, E3, C7 |
| M6 Living world | 24h AI activity with zero Roblox servers, visible on return | **PARTIAL** — F5/F6 built; long-run drill NOT RUN | F7, hosted long-run |
| M7 Endgame | A world is won and a new one opens without filesystem surgery | **NO** | B4 |
| M8 Ops | Restore drill; DB-touching health check; CI blocks a red PR | **NO** — B10a done (local), the rest untouched | B10b (after H6), H5, H7 |

Plus the practice slice's own bar: S-01…S-08 Studio on `0b7a34a`, S-09 phone (Adam), G-01 humans, G-02 decisions — §3.

---

## 6. Boundaries honored this turn

- **No Studio live runs** — they need the PC session spelled out under "Exact prerequisite for any Studio row" (Studio + Rojo + a checkout of the tip, on Adam's PC or in a granted computer-control session); every Studio-dependent row above says NOT RUN rather than borrowing history.
- **No BotDOOR live matrix** — the BotDOOR host/adapter reconciliation and its held-input STOP/focus-loss matrix live outside this repo and stay a separate task; nothing here depends on it.
- **No local backup/restore drill run either** (revisions 1–2) — B10a was written up as the next assigned execution task, not claimed. **Revision 3: B10a has now been run** (`38610e0`, PASS, evidence note linked in the row); it is the only live claim added since, and it certifies the local procedure, not the hosted one.
- **Revision 4: the HTTP-only re-drive has been run** (`21b917b`, 14/14 PASS, evidence note linked in §7). It certifies the world server's HTTP surface at this tip for the rows named above and nothing Roblox-side: no Studio row moved, B6 did not move, and the rows say which sub-claims (warehouse cap, prerequisite refusals, 12 other building types, kill -9) are still tests-only.
- **Revision 5: the B8 kill -9 drill has been run** (`69b7548`, 11/11 PASS, evidence note linked in the row). It certifies SIGKILL recovery of the world server's SQLite WAL at this tip and nothing more: power loss is not simulated, no battle was in progress at the kill, and nothing Roblox-side moved. Of the sub-claims listed in the revision-4 line, kill -9 and one prerequisite refusal are now live; warehouse cap and the other building types are still tests-only.
- **Revision 6: the H1/H2 door drill has been run** (`03a9f41`, 10/10 PASS, evidence note linked in the rows). It certifies the rate limits at their coded defaults on a real clock and the contract-shape guard, over loopback HTTP, and nothing more: not the numbers' fitness, not the door behind a proxy, not the Roblox client's handling of a 429, and not per-player auth (H3 — the drill records the unthrottled link route as H3's problem). Nothing Roblox-side moved.
- **Revision 7: the C2 building ladder drill has been run** (`11f14d6`, 12/12 PASS, evidence note linked in the row). It certifies, over loopback HTTP against seven disposable worlds, that the coded prerequisite rules, caps, queue limit, costs and timers are the ones that run, and that three more building types complete at their instant with the next order starting then — and nothing more: not the Academy's acceptance (the seed knob caps it first), not the nine types never completed live, not construction offline catch-up, not what the client renders, and not whether the numbers are good. The dev seed knob set standing levels; every rule under test ran in the production code path. Nothing Roblox-side moved.
- **Revision 8: the D1–D3/E1 marches + fog drill has been run** (`6e5c9bc`, 10/10 PASS, evidence note linked in the rows). It certifies, over loopback HTTP against one disposable world with three readers on both doors, that the fog zeroes every field for every foreign village on both doors, that the event replay and the live SSE stream drop other kingdoms' private events and fog embedded villages, that an attack is refused until a report exists and `battle.open` is refused when the defender has changed since the report, and that a march is mustered at acceptance, paced by its slowest unit exactly as game-core computes, idempotent under replay (before and after a restart) and caught up correctly after an outage that swallowed its arrival — and nothing more: **no battle was fought**, `battle.open`'s acceptance path was not driven, no attack arrived while the server was down, nothing Roblox-side moved, and the numbers are certified as the coded numbers, not as good ones. The seed knob armed the villages; every rule under test ran in the production code path. C1's warehouse cap was **not** attempted: its row now carries the arithmetic (9.43 h at fixture levels, longer at every seed level) and the ruling that a `last_materialized_at` rewind would be a labelled simulation. Three observations are recorded in the rows and not judged.
- **Revision 9: the battle-settle drill has been run** (`2456b3a`, 10/10 PASS, evidence note linked in the rows). It certifies, over loopback HTTP against one disposable world, that `battle.open` is accepted on current intel and refused three ways otherwise, that field orders are sequenced, capped at five and exempt from the world-version check, that `battle.resolve` and the server's own auto-resolve at `auto_resolve_at` settle through the same path with an outcome that equals game-core's `resolveBattle` on the frozen row **byte for byte**, that prisoners change sides and loot comes home without a soldier or a log being invented, that a second army at the same walls waits, re-checks its intel and loots only what the first left, that the defender is told and the watcher is not — and nothing more: **no kill landed during settlement** (B8's mid-settle variant is the named follow-up, recipe in the note), no retreat, no rams at a wall, no conquest, no attack arriving offline, nothing Roblox-side, and the numbers are certified as the coded numbers, not as good ones. The seed knob armed the villages; every rule under test ran in the production code path. Five observations recorded in the rows and not judged. Four smoke runs and one recorded run failed before the pass, every one on the drill's own oracle or tooling; the note says which.
- **No merge, no push to `main`, no secrets** — this file rides a draft PR on its own branch.
- Historical numbers (core 104 / server 139 / scroll 11, the 2026-08-23 audit run, the 2026-08-29 `c9e1e5c` HTTP exercise, assisted S-01) are labeled historical everywhere they appear, with date and SHA.

## 7. Next execution tasks this matrix assigns (no purchase, no PC required)

| Task | Owner | Closes / advances | Needs |
|---|---|---|---|
| ~~Run B10a (local backup + restore drill) and file a dated `docs/verification/` note~~ **DONE 2026-09-17** (`38610e0`, PASS; `docs/verification/2026-09-17-b10a-local-restore-evidence.md`) — Claude's review of the record still owed | Cursor (ran) / Claude (review) | B10a closed; B10b de-risked | Rerun any time: `node scripts/b10a-local-restore-drill.mjs` |
| ~~Re-drive the 2026-08-29 scratch-DB HTTP exercise on `main @ 9b478db` (link, snapshot, build queue, replay, restart)~~ **DONE 2026-09-20** (`21b917b`, 14/14 PASS; `docs/verification/2026-09-20-http-live-redrive-evidence.md`) — Claude's review of the record still owed | Cursor (ran) / Claude (review) | Current-tip live evidence for A1, A2 (server half), B1, B7, C1, C2 — recorded in those rows | Rerun any time: `node scripts/http-live-redrive.mjs` (~14 min, real timers) |
| ~~Write the kill-9 WAL recovery test the audit describes in §13.2~~ **DONE 2026-09-20** (`69b7548`, 11/11 PASS; `docs/verification/2026-09-20-b8-kill9-wal-recovery-evidence.md`) — delivered as a repeatable drill script, not a `server/test` case, so `server/` stays byte-identical to `main`; Claude's review of the record still owed | Cursor (ran) / Claude (review) | B8 closed for kill -9; power loss and the mid-battle-settle variant remain open on the row | Rerun any time: `node scripts/b8-kill9-wal-recovery-drill.mjs` (~50 s, real 45 s recruit timer) |
| Extend the B8 drill so the kill lands during battle settlement (audit M3's exact wording) | Cursor | B8 (battle-settle variant), M3 | No Studio, no PC. **The recipe is now exact** (`2456b3a`, battle drill steps 1–5 and 8): a battle `open` with `battle.resolve` one call away, or an unattended attack whose `auto_resolve_at` is known to the millisecond; `SIGKILL` with the resolve in flight (record where it landed, as the B8 drill does) or inside the ~100 ms after the deadline; on restart check all-or-nothing across the session row, the march row, the plan row, the defender's row, war points, both notifications and both events — one transaction, so a half-settled battle is a bug. The evidence note's "B8 mid-settle recipe" paragraph lists the columns |
| *(unassigned, picked up)* ~~Drive the rate limits at their production defaults and the contract-shape guard over HTTP~~ **DONE 2026-09-20** (`03a9f41`, 10/10 PASS; `docs/verification/2026-09-20-h1-h2-door-drill-evidence.md`) — Claude's review of the record still owed | Cursor (ran) / Claude (review) | H1, H2 live; M1's two server-side clauses shown on loopback | Rerun any time: `node scripts/h1-h2-door-drill.mjs` (~80 s, real 12 s / 60 s / 2 s refills) |
| *(unassigned, picked up)* ~~Drive C2's other prerequisite rules and complete buildings other than the Timber Camp over HTTP~~ **DONE 2026-09-20** (`11f14d6`, 12/12 PASS; `docs/verification/2026-09-20-c2-building-ladder-evidence.md`) — Claude's review of the record still owed | Cursor (ran) / Claude (review) | C2: every prerequisite rule, queue limit, max-level refusals, Quarry/Warehouse/Farm completions live; C3 observation recorded | Rerun any time: `node scripts/c2-building-ladder-drill.mjs` (~14 min, real 720/780/840 s timers; `LADDER_SKIP_COMPLETION_WAIT=1` for the 5 s ladder-only pass) |
| *(unassigned, picked up)* ~~Drive D1–D3 fog/scouting and E1 march launch over HTTP~~ **DONE 2026-09-20** (`6e5c9bc`, 10/10 PASS; `docs/verification/2026-09-20-d1-d3-e1-marches-fog-evidence.md`) — Claude's review of the record still owed | Cursor (ran) / Claude (review) | D1, D2, D3, E1 live; E2 server half observed; the "scouted target + march in flight" prerequisite for the B8 battle-settle variant is now a working recipe (seed army, Spy, attack at the walls with `auto_resolve_at` set) | Rerun any time: `node scripts/d1-d3-e1-marches-fog-drill.mjs` (~60 s, real 17/24/39/58 s march timers; no battle) |
| *(unassigned, picked up)* ~~Drive the battle itself over HTTP — `battle.open` acceptance, orders, `battle.resolve`, auto-resolve, settlement, loot home~~ **DONE 2026-09-20** (`2456b3a`, 10/10 PASS; `docs/verification/2026-09-20-battle-settle-evidence.md`) — Claude's review of the record still owed; **the B8 mid-settle kill was NOT folded in** and stays on the row above | Cursor (ran) / Claude (review) | E4, E6 live; E5 orders half, D3 acceptance, C1 loot route, B6 fight, M2 server-side auto-resolve live; five observations recorded | Rerun any time: `node scripts/battle-settle-drill.mjs` (~3.5–4.5 min, real 12/17/25/39 s marches, two 120 s waits at the walls, 23/37 s return legs) |

**HTTP-only claims still resting on tests alone after revision 9** (for whoever picks the next slice): **the B8 mid-settle kill** (recipe on the §7 row and in the battle note); **conquest at the current tip** — E10 and B6's second half over HTTP with `KINGSAGE_DEV_SEED_NOBLES` (Counts, escort survival, Realm of Power 294 halved per attack, two waves, capture at 0, the loser re-seated), which also needs no Studio; **`battle.retreat`** and its server-derived exposure (E5's other half — a third attack opened and withdrawn, `retreatSurvivors` recomputed from `opened_at`); **rams' permanent wall drop** (send the seed's Ram at a walled target and read `buildings_json.wall` after); a second battle against a **thinned but standing** garrison (E6's literal wording); C1's warehouse-cap branch (9.43 h of real accrual at fixture levels, longer at every `KINGSAGE_DEV_SEED_LEVEL` rung, arithmetic on the row; a stopped-server rewind of `last_materialized_at` would be a simulation and must be labelled as one); C2's Academy acceptance (needs HQ 10 with the Academy below 3 — no knob gives that; real play or a targeted seed) and completion of the nine building types never finished live (1080 s+ each); an *attack* march arriving during an outage (only a Spy's arrival was caught up offline); C6's over-cap recruit (a known-BROKEN row — a drill would document the bug, not pass); H1 behind a reverse proxy (H6).

Everything else that is NOT RUN waits on the Studio prerequisite (§ "How to read"), a physical phone (Adam), hosting (H6), humans (G-01), or a decision (§4).
