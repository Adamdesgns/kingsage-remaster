# screenshots/

**This folder is empty.** All 37 required screenshots are recorded as
`NOT CAPTURED` in `../13-SCREENSHOT-CATALOG.md`, which also contains exact
capture instructions and the required filenames.

The agent that assembled this package cannot see a screen. Nothing was
fabricated to fill the gap.
