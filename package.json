{
  "name": "kingsage-remaster",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "check:types": "node scripts/check-types.mjs",
    "check:gate-a": "node scripts/check-gate-a.mjs",
    "test:core": "npm --prefix packages/game-core test",
    "test:gate-a": "npm run check:types && npm run check:gate-a && npm run test:core && npm --prefix mobile-rebuild run build",
    "start:world": "npm --prefix server start",
    "test:server": "npm --prefix server test",
    "check:gate-b": "node scripts/check-gate-b.mjs",
    "test:gate-b": "npm run test:gate-a && npm run check:gate-b && npm run test:server",
    "check:gate-c": "node scripts/check-gate-c.mjs",
    "test:gate-c": "npm run test:gate-b && npm run check:gate-c",
    "test:roblox-layer": "npm --prefix server run test:roblox",
    "check:luau": "lune run roblox/scripts/syntax-check.luau",
    "check:luau-rules": "lune run roblox/scripts/rules-check.luau",
    "check:spike-sim": "lune run roblox/scripts/spike-sim-check.luau",
    "test:luau": "npm run check:luau && npm run check:luau-rules && npm run check:spike-sim",
    "check:gate-d": "node scripts/check-gate-d.mjs",
    "test:gate-d": "npm run test:gate-c && npm run check:gate-d"
  },
  "devDependencies": {
    "@types/node": "26.2.0"
  }
}
